# my progression, challenges and memos during the eloquentJS course

## for updating the checksums file, chatgpt made this command line : 
```bash
find . -path ./.git -prune -o -type f -print -exec sha256sum {} \; >> checksums.txt
```
